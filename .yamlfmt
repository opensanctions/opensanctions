---

formatter:
  type: basic
  # > It's not perfect; it uses the best_width setting from the gopkg.in/yaml.v3
  # > library. If there's a very long token that extends too far for the line
  # > width, it won't split it up properly.
  # https://github.com/google/yamlfmt/blob/main/docs/config-file.md#note-on-max_line_length
  max_line_length: 80